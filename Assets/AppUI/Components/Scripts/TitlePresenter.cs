using AppUI.Components.Scripts;

namespace AppUI.PlayerTracker.Scripts {
    public class TitlePresenter : TextBase
    {
        public void SetTitle(string title) {
            SetText(title);
        }
    }
}
